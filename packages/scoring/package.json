{
  "name": "@crypto-analyzer/scoring",
  "version": "1.0.0",
  "description": "High-level scoring orchestration for crypto analyzer",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@crypto-analyzer/types": "file:../types",
    "@crypto-analyzer/core-math": "file:../core-math",
    "@crypto-analyzer/validation": "file:../validation"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vitest": "^1.4.0"
  }
}